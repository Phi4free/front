import { Container } from './styles'

export function ForgotPasswordModal() {
  return (
    <Container>
      <h1>
        Componente que só será utilizado nessa página, então para seguir o padrão react é legal componentizar
        mas como não vai estar em nenhuma outra página esse componente não deve ficar no diretório components
      </h1>
    </Container>
  )
}